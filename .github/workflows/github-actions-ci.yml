name: Node js app deploy to EKS
on:
  push:
    branches: [ "main" ]
    tags-ignore: [ "*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      # with:
      #   fetch-depth: 0 # Important for Semantic Release to analyze commits

    - name: Semantic Release
      id: semantic
      uses: cycjimmy/semantic-release-action@v4
      with:
        extra_plugins: |
          @semantic-release/changelog@6.0.0
        branch: main
      env:
        GITHUB_TOKEN: ${{ secrets.G_TOKEN }}

    - name: Install kubectl
      uses: azure/setup-kubectl@v2.0
      with:
        version: 'v1.24.0'


    - name: Build docker image
      if: steps.semantic.outputs.new_release_published == 'true'
      env:
        REGISTRY: ghcr.io/dabbonet
        REPOSITORY: dipzin-nextjs
        IMAGE_TAG: ${{ steps.set_tag.outputs.new_release_version  }}
      run: |
        docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .

    # - name: Configure AWS Credentials
    #   uses: aws-actions/configure-aws-credentials@v1
    #   with:
    #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    #     aws-region: us-east-1



      #docker push $REGISTRY/$REPOSI

    # - name: Login to Amazon ECR
    #   id: login-ecr
    #   uses: aws-actions/amazon-ecr-login@v1

    # - name: Build, tag, and push docker image to Amazon ECR
    #   env:
    #     REGISTRY: ${{ steps.login-ecr.outputs.registry }}
    #     REPOSITORY: primuslearning
    #     IMAGE_TAG: 1.1
    #   run: |
    #     docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
    #     docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG

    # - name: Update kube config
    #   run: aws eks update-kubeconfig --name primuslearning
    
    # - name: Deploy to EKS
    #   run: |
    #     kubectl apply -f deployment.yaml
    #     kubectl apply -f service.yaml