import type { Meta, StoryObj } from '@storybook/react';
import { HorizontalBreak } from '@/components/Static/restrictions/horizontal-break';

export default {
  title: 'Static/Restrictions/HorizontalBreak',
  component: HorizontalBreak,
} as Meta;

export const Default: StoryObj<typeof HorizontalBreak> = {
};
