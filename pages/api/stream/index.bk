import { NextApiRequest, NextApiResponse } from 'next'
import supabase from '../../../lib/supabase';

// const getShowcase = async ({ id }: { id: number }) => {
//     // console.log('id: ', id);
//     let { data: showcase, error } = await supabase
//         .from('screen')
//         .select('*')
//         .match({ app_id: id, is_showcase: true })

//     // console.log(showcase!.length);
//     if (showcase!.length > 0) {
//         return showcase;
//     } else {
//         return null;
//     }
//     // console.log('showcase: ', showcase);
//     // res.status(200).json(showcase);
// }

export default (_req: NextApiRequest, res: NextApiResponse): void => {



    // getShowcase({ id: 460 });

    const getShowcases = async () => {
        const { data, error } = await supabase
            .from('random_showcases')
            .select()
        res.status(200).json(data);
    }
    getShowcases();

    // const getApplications = async () => {
    //     console.log('getting stream data...');
    //     let { data: applications, error } = await supabase
    //         .from('application')
    //         .select('id')

    //     let finalArray = [];
    //     let processedApps = new Set();
    //     while (finalArray.length < 10) {
    //         let shuffledApps = shuffleArray(applications);
    //         for (let i = 0; i < shuffledApps.length; i++) {
    //             if (processedApps.has(shuffledApps[i].id)) {
    //                 continue;
    //             }
    //             processedApps.add(shuffledApps[i].id);
    //             const showcase = await getShowcase({ id: shuffledApps[i].id });
    //             if (showcase && showcase.length > 0) {
    //                 const shuffledShowcase = shuffleArray(showcase);
    //                 finalArray.push(shuffledShowcase);
    //             }
    //             if (finalArray.length === 10) {
    //                 break;
    //             }
    //         }
    //     }
    //     res.status(200).json(finalArray.slice(0, 10));
    // }
    // getApplications();

}

// function shuffleArray(array) {
//     let i = array.length - 1;
//     for (; i > 0; i--) {
//         const j = Math.floor(Math.random() * (i + 1));
//         const temp = array[i];
//         array[i] = array[j];
//         array[j] = temp;
//     }
//     return array;
// }